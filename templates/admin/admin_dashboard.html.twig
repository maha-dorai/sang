<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Dashboard Admin{% endblock %} - BloodBank</title>
    
    <!-- Même CSS que le site principal -->
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" integrity="sha384-JcKb8q3iqJ61gNV9KGb8thSsNjpSL0n8PARn9HuZOnIxN0hoP+VmmDGMN5t9UJ0Z" crossorigin="anonymous">
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.11.2/css/all.css">
    <link rel="preconnect" href="https://fonts.gstatic.com">
    <link href="https://fonts.googleapis.com/css2?family=Nunito:wght@300;400;600;700;800&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css" />
    <link rel="stylesheet" type="text/css" href="{{ asset('themes/css/style.css') }}">
    <link rel="shortcut icon" href="{{ asset('themes/imgs/favicon.ico') }}">
    
    <!-- Bootstrap Icons pour les icônes modernes -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
    
    <style>
        .admin-wrapper {
            background: #f8f9fa;
            min-height: 100vh;
            font-family: 'Nunito', sans-serif;
        }
        
        /* Sidebar Styles */
        .admin-sidebar {
            background: linear-gradient(135deg, #dc2626 0%, #991b1b 100%);
            color: white;
            min-height: calc(100vh - 70px);
            position: fixed;
            top: 70px;
            left: 0;
            width: 250px;
            padding: 20px 0;
            box-shadow: 2px 0 10px rgba(0,0,0,0.1);
            z-index: 999;
            overflow-y: auto;
        }
        
        .admin-nav .nav-link {
            color: rgba(255,255,255,0.8);
            padding: 12px 20px;
            margin: 5px 10px;
            border-radius: 8px;
            transition: all 0.3s;
            display: flex;
            align-items: center;
        }
        
        .admin-nav .nav-link:hover,
        .admin-nav .nav-link.active {
            background: rgba(255,255,255,0.15);
            color: white;
            transform: translateX(5px);
        }
        
        .admin-nav .nav-link i {
            margin-right: 10px;
            width: 20px;
            text-align: center;
        }
        
        .sidebar-heading {
            color: rgba(255,255,255,0.5);
            font-size: 0.75rem;
            text-transform: uppercase;
            padding: 10px 20px;
            margin-top: 20px;
            letter-spacing: 1px;
        }
        
        /* Main Content avec sidebar */
        .main-content {
            margin-left: 250px;
            padding: 20px;
            margin-top: 70px;
        }
        
        /* Responsive */
        @media (max-width: 768px) {
            .admin-sidebar {
                transform: translateX(-100%);
                transition: transform 0.3s;
            }
            
            .admin-sidebar.show {
                transform: translateX(0);
            }
            
            .main-content {
                margin-left: 0;
            }
            
            footer {
                margin-left: 0 !important;
            }
        }
        
        /* Toggle button pour mobile */
        .sidebar-toggle {
            display: none;
            position: fixed;
            bottom: 20px;
            left: 20px;
            z-index: 1000;
            background: linear-gradient(135deg, #dc2626 0%, #991b1b 100%);
            color: white;
            border: none;
            border-radius: 50%;
            width: 50px;
            height: 50px;
            box-shadow: 0 4px 8px rgba(0,0,0,0.3);
        }
        
        @media (max-width: 768px) {
            .sidebar-toggle {
                display: block;
            }
        }
    </style>
    
    {% block stylesheets %}{% endblock %}
</head>
<body class="admin-wrapper">
    <!-- Navbar identique au site principal -->
    <nav id="mainNavbar" class="navbar navbar-expand-lg navbar-light bg-light fixed-top py-0">
        <div class="container-fluid">
            <a href="{{ path('Accueil') }}" class="img-fluid navbar-brand">
                <img src="{{ asset('themes/imgs/nav-logo.png') }}" class="img-hover" alt="BloodBank Logo">
            </a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="justify-content-end collapse navbar-collapse" id="navbarNavDropdown">
                <ul class="navbar-nav">
                    <li class="nav-item pr-2">
                        <a href="{{ path('Accueil') }}" class="nav-link">
                            <i class="fas fa-home"></i> Accueil
                        </a>
                    </li>
                    <li class="nav-item pr-2">
                        <a href="{{ path('admin_dashboard') }}" class="nav-link active">
                            <i class="fas fa-tachometer-alt"></i> Dashboard Admin
                        </a>
                    </li>
                    <li class="nav-item pr-2">
                        <a href="{{ path('app_logout') }}" class="nav-link">
                            <i class="fas fa-sign-out-alt"></i> Déconnexion
                        </a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <!-- Sidebar Admin -->
    <aside class="admin-sidebar" id="adminSidebar">
        <div class="admin-nav">
            <!-- Dashboard -->
            <h6 class="sidebar-heading">TABLEAU DE BORD</h6>
            <ul class="nav flex-column">
                <li class="nav-item">
                    <a class="nav-link {{ app.request.get('_route') == 'admin_dashboard' ? 'active' : '' }}" 
                       href="{{ path('admin_dashboard') }}">
                        <i class="fas fa-tachometer-alt"></i>
                        Dashboard
                    </a>
                </li>
            </ul>

            <!-- Collectes & Lieux -->
            <h6 class="sidebar-heading">COLLECTES & LIEUX</h6>
            <ul class="nav flex-column">
                <li class="nav-item">
                    <a class="nav-link {{ app.request.get('_route') starts with 'app_lieu' ? 'active' : '' }}" 
                       href="{{ path('app_lieu_index') }}">
                        <i class="fas fa-map-marker-alt"></i>
                        Gestion des Lieux
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {{ app.request.get('_route') starts with 'app_collecte' ? 'active' : '' }}" 
                       href="{{ path('app_collecte_index') }}">
                        <i class="fas fa-calendar-alt"></i>
                        Gestion des Collectes
                    </a>
                </li>
            </ul>

               <!-- Dons -->
            <h6 class="sidebar-heading">DONS</h6>
            <ul class="nav flex-column">
                <li class="nav-item">
                    <a class="nav-link {{ app.request.get('_route') == 'admin_don_valider' ? 'active' : '' }}" 
                       href="{{ path('admin_don_valider') }}">
                        <i class="fas fa-check-circle"></i>
                        Validation des Dons
                    </a>
                </li>
            </ul>

            <!-- Stock & Stats -->
            <h6 class="sidebar-heading">STOCK & STATS</h6>
            <ul class="nav flex-column">
                <li class="nav-item">
                    <a class="nav-link {{ app.request.get('_route') starts with 'app_stock' ? 'active' : '' }}" 
                       href="{{ path('app_stock_index') }}">
                        <i class="fas fa-tint"></i>
                        Gestion des Stocks
                    </a>
                </li>

 
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_logout') }}">
                        <i class="fas fa-sign-out-alt"></i>
                        Déconnexion
                    </a>
                </li>
            </ul>
        </div>
    </aside>

    <!-- Toggle button pour mobile -->
    <button class="sidebar-toggle" onclick="toggleSidebar()">
        <i class="fas fa-bars"></i>
    </button>

    <!-- Contenu principal -->
    <div class="main-content">
        <div class="container-fluid">
            {% block content %}{% endblock %}
        </div>
    </div>

    <!-- Footer identique au site principal -->
    <footer id="contact-us" class="mt-5" style="margin-left: 250px;">
        <div class="container">
            <div class="row align-items-center">
                <div class="col-sm-5 center vertical-center">
                    <img src="{{ asset('themes/imgs/logo.png') }}" class="footer-logo img-fluid img-hover" alt="Logo">
                </div>
                <div class="col-4 col-sm-2 center">
                    <ul>
                        <li>
                            <p><a href="{{ path('Accueil') }}">Accueil</a></p>
                        </li>
                        <li>
                            <p><a href="{{ path('admin_dashboard') }}">Dashboard Admin</a></p>
                        </li>
                    </ul>
                </div>
                <div class="col-8 col-sm-5 center">
                    <ul>
                        <li>
                            <p><i class="fab fa-instagram mr-2"></i><a href="#">bloodbank_tn</a></p>
                        </li>
                        <li>
                            <p><i class="fab fa-facebook mr-2"></i><a href="#">BloodBank Tunisia</a></p>
                        </li>
                        <li>
                            <p><i class="fab fa-twitter mr-2"></i><a href="#">@BloodBankTN</a></p>
                        </li>
                    </ul>
                </div>
            </div>
        </div>
    </footer>

    <!-- Scripts identiques -->
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js" integrity="sha384-DfXdz2htPH0lsSSs5nCTpuj/zy4C+OGpamoFVy38MVBnE+IbbVYUew+OrCXaRkfj" crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.1/dist/umd/popper.min.js" integrity="sha384-9/reFTGAW83EW2RDu2S0VKaIzap3H66lZH81PoYlFhbGU+6BZp6G7niu735Sk7lN" crossorigin="anonymous"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js" integrity="sha384-B4gt1jrGC7Jh4AgTPSdUtOBvfO8shuf57BaghqFfPlYxofvL8/KUEfYiJOMMV+rV" crossorigin="anonymous"></script>
    
    <script>
        function toggleSidebar() {
            document.getElementById('adminSidebar').classList.toggle('show');
        }
    </script>
    
    {% block javascripts %}{% endblock %}
</body>
</html>